import { Flex, Title } from "@mantine/core";

export function DashboardPage() {
    return (
        <Flex display={"flex"} p={10} justify={"space-between"}>
            <Title order={3}>Dashboard</Title>
            <Flex>
            <Title order={3}>Dashboard</Title>
            </Flex>
        </Flex>
    )
}